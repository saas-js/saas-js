const baseUrl = 'https://github.com/saas-js/saas-js'

const docsConfig = {
  repo: {
    url: baseUrl,
    editUrl: `${baseUrl}/edit/main/website/pages`,
    blobUrl: `${baseUrl}/blob/main`,
  },
}

export default docsConfig
